# Nowshin Language — TODO

## Plan Step Tracking
- [x] 1. Refactor web IDE: move inline CSS/JS out of `index.html` into `style.css` and `app.js`, keep single source of truth.

- [ ] 2. Align web IDE keyword set + transpiler rules with native lexer keywords.
- [ ] 3. Harden transpiler execution + improve errors in browser.
- [ ] 4. Improve native compiler build pipeline (`main.cpp`, `build.bat`) with better errors.
- [ ] 5. Improve native generator/lexer consistency where needed.
- [ ] 6. Add/adjust examples and ensure `build.bat` runs deterministically.
- [ ] 7. Rewrite `README.md` into a clean project doc.

Progress Notes:
- Prioritization confirmed: correctness/consistency of native compiler (A) and both equally only after that baseline is solid.

