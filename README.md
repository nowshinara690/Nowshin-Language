# Nowshin-Language
Nowshin Language is a custom programming language developed using C++. The project works as a mini compiler that converts user-defined keywords into C++ code and then compiles it into an executable program. It includes lexical analysis, token generation, and code generation phases.
